export type productsType = {
    title: string,
    price: number,
    imageUrl: string,
    description?: string,
    _id?: string
}

export type propsType = {
    name?: string,
    alt: string,
    price?: number,
    image: string
    desc?: string,
    id?: string,
    key?:string
  }

export type twoType = {
    imageUrl: string
    title: string,
    id?: string,
  }

  export type headType = {
    head?: string
  }