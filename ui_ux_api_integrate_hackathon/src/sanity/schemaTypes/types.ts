export type productsType = {
    title: string,
    price: number,
    imageUrl: string,
    description?: string,
    slug?: string
}

export type propsType = {
    name?: string,
    alt: string,
    price?: number,
    image: string
    desc?: string,
    id?: string
  }

export type twoType = {
    imageUrl: string
    title: string,
  }

  export type headType = {
    head?: string
  }